name: Daily weather build
on:
  schedule:
    - cron: '0 5 * * *'   # chaque jour à 5h UTC (7h Paris en été)
  workflow_dispatch:

jobs:
  trigger-vercel:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy
        run: curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}
